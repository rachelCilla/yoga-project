import React from 'react'
import { useState } from 'react';

export default function FavoritePosesList({handleBackButtonClick, favoritePose}) {
//   const [cookies, setCookie, removeCookie] = useCookies(null);
//   const editMode = mode === 'edit'? true : false;

//   const [data, setData] = useState({
//     user_email: editMode? pose.user_email: cookies.Email,
//     pose_name: editMode ? pose.pose_name: null,
//     date: editMode ? pose.date: new Date(),
//   });

//   const postData = async (e) => {
//     e.preventDefault();
//     try {
//       const response = await fetch(`${process.env.REACT_APP_SEVERURL}/favorite_poses`, {
//         method 



  return (
    <div className='favorite-poses-list'>
      
    {/* //     <h1 className=''>Favorite Poses List</h1>
    //     <button  onClick={handleBackButtonClick}>Back</button>  

    // <div className="">
    //     <h3>{favoritePose.pose_name}</h3>
       
    //     <img src={...} alt="pose image" />
    //         <button>Remove from Favorites</button>
    //         <button >More Info</button>
    //     </div> */}
    </div>
  )
}
